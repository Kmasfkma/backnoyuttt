fastapi<0.100.0
uvicorn
python-dotenv
psutil
redis
# نثبت نسخة قديمة من supabase تتوافق مع httpx 0.24.1 لمنع الـ Resolution Loop
supabase==2.0.3
structlog
websockets
requests
# --- مكتبات Kortix AI ---
playwright
tavily-python
# نثبت SDK فقط بإصدار قديم ونتركه يدير تبعياته (مع Mocking في Dockerfile)
# daytona-sdk removed - using mocks in Dockerfile
# Langfuse القديم يحتاج httpx قديم
langfuse<2.0.0
# تثبيت httpx يتوافق مع langfuse ومع daytona القديم
httpx==0.24.1
watchtower
boto3
pydantic==1.10.17
litellm<1.0.0
# --- مكتبات المصادقة والأمان ---
PyJWT
passlib[bcrypt]
python-multipart
cryptography
# --- مكتبات مساعدة إضافية ---
aiohttp
# asyncio removed (stdlib)
# --- قواعد البيانات ---
sqlalchemy
asyncpg
psycopg[binary]
# --- المدفوعات والخدمات الخارجية ---
stripe
sentry-sdk
firecrawl-py==2.16.5
# exa-py removed - conflicts with httpx/httpcore pins
# composio-core removed - requires pydantic v2
replicate==0.26.0
# mailtrap removed - requires pydantic v2
novu-py==0.4.2
# --- بروتوكول MCP ---
# mcp removed - requires pydantic v2
